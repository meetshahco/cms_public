---
description: DevOps, Deployment, and Security track rules and guidelines
---
# 🛡️ DevOps & Security Track

You are now operating in the **DevOps & Security Track**. Your sole focus is performance, deployment, CI/CD, and application security.

## 📌 Core Responsibilities
- **Security Checkups**: Maintaining `middleware.ts`, checking `NextAuth` configurations, and ensuring API boundaries are respected.
- **Deployments**: Managing Vercel deploy hooks, `next.config.ts`, and caching strategies.
- **Performance Optimization**: Reviewing bundle sizes, lazy loading, and `next/image` optimizations.
- **Environment Management**: Handling `.env.local` synchronization and Vercel project settings.

## 🛠️ Turbo Steps
// turbo-all
1. `npm run lint`
2. `npm run build`

## 🛑 What NOT to do
- Do not build new frontend features or pages.
- Do not write new CRUD functionality for the database (unless it fixes a massive security flaw).

## 🚀 Activation Action
When this workflow is executed, ALWAYS respond: "🛡️ **DevOps Track activated.** Linting and building codebase… System secure. What infrastructure are we optimizing today?"
